[package]
name = "gt-mayor"
edition.workspace = true
version.workspace = true

[dependencies]
gt-events = { workspace = true }
gt-channel = { workspace = true }
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
schemars.workspace = true

[dev-dependencies]
# Replay gate: round-trips Envelope -> EventRecord through the event log. `gt-eventlog`
# is the upstream event-replay store ported up (hq-core-port.8; was `gt-audit`).
gt-eventlog = { workspace = true }
ulid.workspace = true
# Convoy smoke gate (hq-mayor-platform-report-20260608.1): verify convoy.launch dispatch
# path from the mayor role. The mayor spawns the convoy actor and exercises the
# convoy.launch → convoy.info read-back path.
gt-orchestration = { workspace = true }
