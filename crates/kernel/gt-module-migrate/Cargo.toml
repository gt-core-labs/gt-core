[package]
name = "gt-module-migrate"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Per-module SQL migration loader. sqlx multi-source aggregation."

[dependencies]
sqlx = { workspace = true }
gt-module = { workspace = true }

[dev-dependencies]
# Async runtime for the GT_PG_URL-gated apply/disable/reactivate/purge
# integration test (hq-mod-migrate.5).
tokio = { workspace = true }
# Ephemeral-PG guard (gtcore-d43955): fail-closed before any test statement.
gt-store-pg = { workspace = true }
