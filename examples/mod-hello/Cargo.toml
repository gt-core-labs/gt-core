[package]
name = "mod-hello"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Smallest viable GtModule — learn-by-example reference. Lands in hq-mod-docs.2."

[dependencies]
gt-module = { workspace = true }
axum = { workspace = true }
serde_json = { workspace = true }
semver = { workspace = true }

[dev-dependencies]
# The end-to-end gate assembles the module through the real RootBuilder and
# drives a Dog worker through the receipt path, so the proof exercises the whole
# on-ramp — not just the crate in isolation.
gt-dog = { workspace = true }
tokio = { workspace = true }
tower = { workspace = true }
async-trait = { workspace = true }
